program programa;

	var a : int;
	var b : float;

	function start() void {return;}
	
	function testTwo(a: int, hello : int, helloTwo : float) int {
		var local: Car;
		var array[1]: int;
		var mat[1][2]: float;

		if (a || b && c > b) {
			write("helloworld");
		}
		return 10;
	}

main() {
		var c : float = 1.0;
		var x : char = "a";

		if (a > z) {
			read(car.doors, a, b);
		} else {
			write("hello world");
		}

        return;
}