program programa;

	class Car {
		attributes
		var doors: int;

		methods
		function start() void {
			return;
		}

	};

	var a : int;
	var b : float = 1 < 2;
	
	function start() void {return;}
	
	function testTwo(a: int, hello : int, helloTwo : float) int {
		var local: string;
		var array[1]: int;
		var mat[1][2]: float;

		if (a || b && c) {
			write("helloworld");
		}
		return 10;
	}

	main() {
		var c : float = a + b;
		var x : char = "x";

        a = test();

		if (a > z) {
			read(car.doors, a, b);
		} else {
			write("hello world");
		}

        return;
	}